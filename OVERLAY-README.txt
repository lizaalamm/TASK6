Paste these files INTO your existing cloned TASK6 folder
(the one that already has node_modules).

Path example:
  ...\projects\TASK6\

Overwrite when Windows asks.

You do NOT need to reinstall frontend node_modules.

Then only install backend once:
  cd backend
  npm install
  npm run dev

Other terminal (your existing frontend):
  npm run dev
